# Summary: <feature slug>

<!-- The page a READER gets first: `tools/kb.sh show <slug>` and the contents
     page (`tools/kb.sh index`) print it before anything else, so a human reads
     this instead of six stage files. It is NOT a stage artifact and no approval
     binds it — that is deliberate, so it can stay TRUE as the feature moves:
     written with the intent (Problem, Tags; Cause/Change as far as known),
     revised when build learns better, and Result/Lesson set at ship from what
     delivery.md and evidence.md prove. Plain language, ten lines at most.
     This page STATES; the records PROVE — point at them (evidence.md §n,
     delivery.md, a lesson file) rather than restating their output. -->

- Tags: <domain areas, comma-separated — how a reader browses the contents page and an Obsidian tag pane>
- Problem: <one sentence — what breaks, for whom>
- Cause: <one sentence, or "not known yet">
- Change: <one sentence — what changes, where>
- Result: <one sentence — what is different now; "not delivered" until delivery.md confirms it>
- Lesson: <one sentence, or "none">
