# process residue — session evidence, never framework
.verify/
.impeccable/
.sdlc/
scratch/
.DS_Store
# tools/_run.py is executed, never imported; this only appears if someone
# byte-compiles it by hand
__pycache__/
