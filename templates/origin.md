# Origin: <feature slug>

<!-- The request as it was made, snapshotted BEFORE the intent gate. The intent
     approval binds this file, and so does every gate downstream of it: a
     rewrite here closes the gates by design — an edited ticket is a new
     decision, not drift. Verbatim where it matters (acceptance criteria,
     screens, fields, wording); bulk attachments go to scratch/ and are cited
     here. Omit the file when the request has no origin beyond the chat —
     intent.md's O-items then carry the human's words. -->

- Ref: <ticket key / URL / 기획서 path>
- Read with: <the tool or command, e.g. `jira issue view A20-1234`, `cat docs/spec.md`>
- Snapshot: YYYY-MM-DDTHH:MM:SSZ

## Text
<the request as written — description, acceptance criteria, screens, wording>
